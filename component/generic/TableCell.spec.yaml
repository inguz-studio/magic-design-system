component: TableCell
version: 0.1.0
kind: generic
status: draft
tenants: [shelflix]
category: display
atomic_level: atom

vendor:
  primitive: null
  install_path: null
  import_alias: null
  note: "Elemento HTML nativo <td>"

variants:
  type: [text, number, status, action]

sizes: []
states: [default]

tokens:
  shared:
    padding-inline: --semantic-space-inset-lg
    padding-block: --semantic-space-inset-md
    typography: --primitive-text-sm
    border-bottom: --semantic-border-subtle

  text:    { text: --semantic-text-primary, align: left }
  number:  { text: --semantic-text-primary, align: right, font-feature: "tabular-nums" }
  status:  { composes: "Tag (variant=tint, size=sm)" }
  action:  { composes: "IconButton (variant=ghost, size=sm)", align: right }

rhythm:
  internal_item_gap: n/a
  internal_group_gap: n/a
  container_padding: inset-md
  heading_association_respected: true
  first_last_reset_required: false
  exceptions: []

a11y:
  semantic_element: td
  wcag_criteria: ["1.3.1", "1.4.3"]

api:
  props:
    type: { type: enum, values: [text, number, status, action], default: text }
    status: { type: TagStatus, required: false }
    children: { type: ReactNode, required: true }

outputs: [tsx, types, contract, stories]

composes: [Tag, IconButton]

gaps_for_foundations: []

references:
  squad_spec: squads/magic-ds/component/TableCell.spec.yaml

meta:
  owner: mds-component
  created: 2026-05-14
  last_modified: 2026-05-14
  round: 2
  wave: 2


# Round 3.1 type roles consumidos (--semantic-type-*):
tokens_type:
  - body

# Next required step: mds-audit *design-check --target TableCell.spec.yaml.
# Cannot proceed to mds-ops until score ≥ B.
